node_modules
dist
# El dist del paquete vendorizado SÍ debe entrar al contexto (el Docker build no
# lo recompila; sin él tsc no resuelve structure-verifier).
!vendor/structure-verifier/dist
.env
.env.*
.git
*.log
# Gestor de paquetes: la imagen usa pnpm (pnpm-lock.yaml). El package-lock.json
# quedó obsoleto y no debe entrar al contexto para no confundir el build.
package-lock.json
coverage
.vscode
scripts
# NOTA: vendor/ (directorio vendorizado de structure-verifier) SÍ debe ir al contexto.
